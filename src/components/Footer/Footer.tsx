import * as React from "react";
import "./style.scss";

interface IProps {

}
const Footer: React.FC<IProps> = (IProps) => {
  return (
    <div className="Footer">
      Footer
    </div>
  );
};

export default Footer;
